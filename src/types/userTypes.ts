// src/types/userTypes.ts
export type User = {
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    avatar?: string;
    createdAt?: string;
    updatedAt?: string;
    // Add any other user properties you need
  };